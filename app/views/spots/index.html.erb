<h1> Hello World! <h1>

<%= button_to "Show accessible spaces", '/spots', method: :get, params: { accessibility: 'yes' } %>
<%= button_to "Show non-accessible spaces", '/spots', method: :get, params: { accessibility: 'no' } %>


  <% @spots.each do |spot| %>
<li> <%= spot.label %> , <%= spot.occupied %> ,
  <%= spot.accessible %>,
  <%= spot.spot_information %>,
  <%= spot.parking_area_id %>
<% end %>

